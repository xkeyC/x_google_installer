name: x_google_installer
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  hive: ^2.0.3
  hive_flutter: ^1.0.0
  font_awesome_flutter: ^9.0.0
  dio: ^4.0.0
  device_info: ^2.0.0
  cached_network_image: ^3.0.0
  external_app_launcher: ^0.0.1
  app_installer:
    git:
      url: https://github.com/xkeyC/app_installer.git
  flutter_web_browser: ^0.14.0
  path_provider: ^2.0.1
  installed_apps: ^1.1.1
  qr_flutter: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
  assets:
    - assets/appLogo.png
flutter_intl:
  enabled: true